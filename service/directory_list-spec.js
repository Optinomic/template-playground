describe('directoryList', function() {

  beforeEach(module('optinomic'));

  it('should ...', inject(function(directoryList) {

	//expect(directoryList.doSomething()).toEqual('something');

  }));

});